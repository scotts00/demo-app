apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: marketing-ingress
  namespace: default
  annotations:
    kubernetes.io/ingress.class: nginx
spec:
  rules:
  - host: marketing.localhost
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: marketing-service
            port:
              number: 80